\indent {The goal is to implement a basic HTTP server that supports directory listing, static HTML files, user file uploads. The server needs to run on a Linux server environment, such as those supported in our lab machines. Specifically, your web server needs to support the following features:
}
\indent \indent \begin{itemize}
    \item HTTP requests with query and header parsing
    \item HTML page navigation 
    \item Static file transport allowing users to submit a file to the server side
\end{itemize}